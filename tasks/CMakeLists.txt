# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(net_mikrotikTaskLib)
ADD_LIBRARY(${NET_MIKROTIK_TASKLIB_NAME} SHARED
    ${NET_MIKROTIK_TASKLIB_SOURCES})
add_dependencies(${NET_MIKROTIK_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${NET_MIKROTIK_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${NET_MIKROTIK_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${NET_MIKROTIK_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${NET_MIKROTIK_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${NET_MIKROTIK_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${NET_MIKROTIK_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${NET_MIKROTIK_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${NET_MIKROTIK_TASKLIB_HEADERS}
    DESTINATION include/orocos/net_mikrotik)

