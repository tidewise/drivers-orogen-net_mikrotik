# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(network_status_monitoringTaskLib)
ADD_LIBRARY(${NETWORK_STATUS_MONITORING_TASKLIB_NAME} SHARED
    ${NETWORK_STATUS_MONITORING_TASKLIB_SOURCES})
add_dependencies(${NETWORK_STATUS_MONITORING_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${NETWORK_STATUS_MONITORING_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${NETWORK_STATUS_MONITORING_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${NETWORK_STATUS_MONITORING_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${NETWORK_STATUS_MONITORING_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${NETWORK_STATUS_MONITORING_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${NETWORK_STATUS_MONITORING_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${NETWORK_STATUS_MONITORING_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${NETWORK_STATUS_MONITORING_TASKLIB_HEADERS}
    DESTINATION include/orocos/network_status_monitoring)

